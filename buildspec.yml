phases:
  install:
    commands:
      - echo install.
      - npm install
  pre_build:
    commands:
      - echo pre build...
  build:
    commands:
      - echo Build started on `date`
      - echo Compiling the Node.js code
  post_build:
    commands:
      - echo Build completed on `date`
